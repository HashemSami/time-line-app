import { FC } from "react";

import { HeaderContainer } from "./Header.styles";

const Header: FC = () => {
  return (
    <HeaderContainer>
      <h1>test Header</h1>
    </HeaderContainer>
  );
};

export default Header;
